{
  "name": "ds-poc",
  "private": true,
  "version": "0.0.0",
  "description": "POC comparing two ways of implementing the same design system in Vue: reka-ui directly vs shadcn-vue",
  "scripts": {
    "build:tokens": "pnpm --filter @ds-poc/tokens build",
    "storybook:reka": "pnpm --filter ds-reka storybook",
    "storybook:shadcn": "pnpm --filter ds-shadcn storybook",
    "storybook": "pnpm build:tokens && concurrently -n reka,shadcn -c blue,magenta \"pnpm storybook:reka\" \"pnpm storybook:shadcn\""
  },
  "devDependencies": {
    "concurrently": "10.0.5"
  },
  "engines": {
    "node": ">=20"
  }
}
